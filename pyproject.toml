[project]
name = "redundant-calhacks"
version = "0.1.0"
description = "Redundant: multi-agent cost firewall demo with labelable cache-reuse data generation."
requires-python = ">=3.10"
dependencies = [
  "langcache>=0.13.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
