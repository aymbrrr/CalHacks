# Redis Cloud (sponsor) — must have RediSearch / vector indexing enabled.
# Format: redis://default:<password>@<host>:<port>  (or rediss:// for TLS)
REDIS_URL=redis://default:password@localhost:6379

# OpenAI — used only for embeddings (text-embedding-3-small).
OPENAI_API_KEY=sk-...

# Anthropic — used for redundant.llm() executions.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# REDUNDANT_SIM_THRESHOLD=0.86
# REDUNDANT_DEFAULT_MODEL=claude-haiku-4-5
